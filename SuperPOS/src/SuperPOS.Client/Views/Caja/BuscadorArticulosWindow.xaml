<ui:FluentWindow x:Class="SuperPOS.Client.Views.Caja.BuscadorArticulosWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Buscar Artículo"
        Width="720" Height="520"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ui:TitleBar Grid.Row="0" Title="Buscar Artículo"/>
        <Grid Grid.Row="1" Margin="12,8,12,4">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <ui:TextBox x:Name="TxtBuscar" Grid.Column="0" PlaceholderText="Descripción, código o EAN..." Margin="0,0,8,0" FontSize="14" KeyDown="TxtBuscar_KeyDown"/>
            <ui:Button Grid.Column="1" Content="Buscar" Click="BtnBuscar_Click"/>
        </Grid>
        <DataGrid x:Name="DgResultados" Grid.Row="2" Margin="12,4" AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single" GridLinesVisibility="Horizontal" RowHeight="30" FontSize="13"
                  MouseDoubleClick="DgResultados_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Código" Binding="{Binding CodigoBarras}" Width="110"/>
                <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="*"/>
                <DataGridTextColumn Header="Precio" Width="90">
                    <DataGridTextColumn.Binding><Binding Path="PrecioVenta" StringFormat="N2"/></DataGridTextColumn.Binding>
                    <DataGridTextColumn.ElementStyle><Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Right"/></Style></DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Stock" Width="70">
                    <DataGridTextColumn.Binding><Binding Path="StockActual" StringFormat="N2"/></DataGridTextColumn.Binding>
                    <DataGridTextColumn.ElementStyle><Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Right"/></Style></DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="12,8">
            <Button Content="Cancelar" Width="90" Height="34" Margin="0,0,8,0" Click="BtnCancelar_Click"/>
            <ui:Button Content="Seleccionar" Appearance="Primary" Width="110" Height="34" Click="BtnSeleccionar_Click"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
